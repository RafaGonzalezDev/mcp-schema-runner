# fixtures-workspace

Directorio usado por el fixture `filesystem` (servidor MCP
`@modelcontextprotocol/server-filesystem`).

Este es un subdirectorio real del repositorio para que el fixture
funcione nada más arrancar la app sin requerir configuración adicional.

Puedes añadir archivos aquí para experimentar con el runner.
